{
  "name": "docker-multirun",
  "version": "1.0.1",
  "description": "Run a docker container via docker-run multiple times. Works like docker-compose by keeping the container and reusing the volumes.",
  "repository": "dword-design/docker-multirun",
  "license": "MIT",
  "author": "Sebastian Landwehr <info@dword-design.de>",
  "main": "dist/index.js",
  "bin": {
    "docker-multirun": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "check-unknown-files": "base check-unknown-files",
    "commit": "base commit",
    "dev": "base dev",
    "lint": "base lint",
    "prepare": "base prepare",
    "prepublishOnly": "base prepublishOnly",
    "test": "base test"
  },
  "dependencies": {
    "@dword-design/functions": "^1.2.0",
    "execa": "^5.0.0"
  },
  "devDependencies": {
    "@dword-design/base": "^7.0.9",
    "fs-extra": "^9.1.0",
    "uuid": "^8.3.2",
    "with-local-tmp-dir": "^2.2.15"
  },
  "publishConfig": {
    "access": "public"
  },
  "baseConfig": {
    "usesDocker": true
  }
}
